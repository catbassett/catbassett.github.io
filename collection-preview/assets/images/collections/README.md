# Adding a collection

Create a folder named `01-florals` (or another numbered, lower-case slug) inside a category folder such as `prints` or `embroideries`.

Add `cover.jpg`, an optional `hero.jpg`, and the gallery in numbered order: `01-painting.jpg`, `02-repeat.jpg`, and so on. Supported formats are JPG, JPEG, PNG and WebP.
